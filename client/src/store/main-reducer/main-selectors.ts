import { ReducerName, ReducerType } from '../store';

// export const getMainType = (state: ReducerType) => state[ReducerName.Main].type;

// export const getMainTitles = (state: ReducerType) => state[ReducerName.Main].titles;

// export const getMainTabs = (state: ReducerType) => state[ReducerName.Main].tabsContent;
